cTESTCOMMAND: mppl -w
c A test of implicit statement handling
c And various styles and
c placement of comments 1.0 <- test literal constant in comment
c comment
c Comment
c comment
c comment

      implicit real (a-h,o-z)
c Junk inserted to make lexer change state
      real x
      x = real(a) + 1.0
      implicit integer (i-n)
c test comment
      real x
ctest comment
      x = real(a) + 1.0
c test comment
      implicit real (a-h,o-z)
c test comment
      implicit real (a-h,o-z)
      implicit doubleprecision (a-h,o-z)
      real x
      x = real(a) + 1.0
      implicit integer (i-n)
      implicit integer (i-n)
      implicit integer (i-n)
      real x
      x = real(a) + 1.0
      implicit real(a-h,o-z), integer(i-n)
      real x
      x = real(a) + 1.0
      implicit integer(i-n), real(a-h,o-z), complex(c)
      real x
      x = real(a) + 1.0
